{
  "name": "awesome-weave",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "generate": "bun run generate:base && bun run generate:writereadme && bun run generate:people",
    "generate:base": "bun ./src/scripts/fetch_projects.ts",
    "generate:writereadme": "bun ./src/scripts/fill_readme_projects.ts",
    "generate:people": "bun ./src/scripts/generate_people_svg.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "node-html-parser": "^6.1.13"
  }
}